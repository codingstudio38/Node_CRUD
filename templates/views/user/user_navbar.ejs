<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="http://localhost:5000/user/home"
        style="color: white; border: 1px solid; background: green; padding: 2px; font-weight: 500;">NodeJS</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item <% if (active=='user_home' ) { %> active <% } %>">
                <a class="nav-link" href="/user/home">Home</a>
            </li>
            <li class="nav-item <% if (active=='multiples' ) { %> active <% } %>">
                <a class="nav-link" href="/user/upload-multiples">Multiples Upload</a>
            </li>

        </ul>
        <form class="form-inline my-2 my-lg-0">
            <% if (login) { %>
                <a href="/user/logout" class="btn btn-outline-success my-2 my-sm-0" type="button"
                    style="background-color: #27a127; color: white;">
                    <%= user.name %> <i class="fa fa-sign-out" aria-hidden="true"></i>
                </a>
                <% } else { %>
                    <a href="/user/logout" class="btn btn-outline-danger my-2 my-sm-0" type="button"
                        style="background-color: #27a127; color: white;">
                        Logout <i class="fa fa-sign-out" aria-hidden="true"></i>
                    </a>
                    <% } %>
        </form>
    </div>
</nav>